import React, { Component } from 'react';
import Redirect from '../Components/Redirect';

export default class RegisterSuccess extends Component {
    render() {
        return (
            <Redirect msg="感谢您的注册，此处省略一万字..."/>
        );
    }
}
